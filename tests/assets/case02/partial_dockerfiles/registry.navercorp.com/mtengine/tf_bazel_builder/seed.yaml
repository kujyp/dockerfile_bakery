dockerfile_template: seed.Dockerfile
from: "{{ tf_bazel_builder_base }}"
entities:
 - compute_capabilities
 - xla
imagename: tf_bazel_builder
tagname: "{{ FROM_TAGNAME }}_{{ xla }}_{{ compute_capabilities }}"
